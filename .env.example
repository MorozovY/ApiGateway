# =============================================================================
# ApiGateway Environment Variables
# =============================================================================
# Story 13.4: Vault Integration
#
# ВАЖНО: В production secrets должны храниться в Vault, а не в .env файле!
# Этот файл используется только для local development.
#
# Для получения secrets из Vault (production/CI):
#   1. Получить Vault token: vault login
#   2. Или использовать AppRole: source ./docker/gitlab/vault-secrets.sh

# =============================================================================
# Vault Configuration (optional for local dev)
# =============================================================================
# Если Vault доступен локально, можно использовать его вместо hardcoded values
# VAULT_ADDR=http://localhost:8200
# VAULT_TOKEN=<your-vault-token>

# =============================================================================
# Database (fallback values for local development)
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=gateway
POSTGRES_USER=gateway
POSTGRES_PASSWORD=gateway

# =============================================================================
# Redis (fallback values for local development)
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379

# =============================================================================
# Backend Services
# =============================================================================
GATEWAY_ADMIN_PORT=8081
GATEWAY_CORE_PORT=8080

# =============================================================================
# Keycloak (Identity Provider)
# =============================================================================
KEYCLOAK_ENABLED=false
KEYCLOAK_URL=http://localhost:8180
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# =============================================================================
# DEPRECATED — НЕ ИСПОЛЬЗУЙТЕ (будут удалены в следующей версии)
# =============================================================================
# JWT_SECRET и ADMIN_PASSWORD использовались до миграции на Keycloak (Story 12.x)
# После миграции на Keycloak эти переменные НЕ используются приложением.
# Оставлены временно для обратной совместимости legacy скриптов.
#
# ВАЖНО: НЕ устанавливайте эти значения в production!
# JWT_SECRET=<deprecated>
# ADMIN_PASSWORD=<deprecated>

# =============================================================================
# Frontend
# =============================================================================
VITE_API_URL=http://localhost:8081/api/v1
