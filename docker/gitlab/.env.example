# GitLab Configuration Example
# Скопируйте этот файл в .env и заполните значения
#
# Использование:
#   cp .env.example .env
#   # Отредактируйте .env
#   docker-compose up -d

# GitLab Runner Registration Token
# Получить: GitLab Admin → CI/CD → Runners → New instance runner
GITLAB_RUNNER_TOKEN=glrt-xxxxxxxxxxxxxxxxxxxx

# Опционально: начальный пароль root (только при первом запуске)
# Если не указан, GitLab сгенерирует автоматически
# GITLAB_ROOT_PASSWORD=your_secure_password

# Опционально: внешний URL (по умолчанию http://localhost:8929)
# GITLAB_EXTERNAL_URL=http://localhost:8929
