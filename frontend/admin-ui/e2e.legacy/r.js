const fs = require('fs'); let c = fs.readFileSync('epic-7.spec.ts', 'utf8'); c = c.replace(/const response = await page\.request\.post\(\)/g, "const response = await apiRequest(page, 'POST', \)"); c = c.replace(/const response = await page\.request\.post\(\)/g, "const response = await apiRequest(page, 'POST', \)"); c = c.replace(/const response = await page\.request\.post\(\, \{\s*data: \{ reason \},\s*\}\)/g, "const response = await apiRequest(page, 'POST', \, { reason })"); c = c.replace(/const response = await page\.request\.delete\(\)/g, "const response = await apiRequest(page, 'DELETE', \)"); fs.writeFileSync('epic-7.spec.ts', c, 'utf8'); console.log('Updated epic-7');
