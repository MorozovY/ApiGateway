# Development Environment (для локальной разработки и E2E тестов)
# Переопределяет .env когда запущен через npm run dev

# Keycloak Server URL (прямой доступ для dev)
VITE_KEYCLOAK_URL=http://localhost:8180

# Keycloak Realm name
VITE_KEYCLOAK_REALM=api-gateway

# Keycloak Client ID
VITE_KEYCLOAK_CLIENT_ID=gateway-admin-ui

# Grafana URL
VITE_GRAFANA_URL=http://localhost:3001

# API URL для Vite proxy (gateway-admin:8081 внутри Docker сети)
VITE_API_URL=http://gateway-admin:8081
